[project]
name = "hackathonproject"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "ollama>=0.4.0",
    "rouge-score>=0.1.2",
    "scikit-learn>=1.3.0",
]

[tool.setuptools]
py-modules = ["main", "data", "rag", "ollama_client", "evaluator", "config", "epochs"]
